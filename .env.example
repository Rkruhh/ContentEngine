# Chat / draft / eval (Groq OpenAI-compatible API)
GROQ_API_KEY=

# Local RAG embeddings (no API key — Transformers.js downloads/caches the model on first use)
# Default: Xenova/all-MiniLM-L6-v2 (~23MB quantized)
EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2
